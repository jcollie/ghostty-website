---
title: 3: Set X Property
description: Set X property on the top-level window.
---

<VTSequence sequence={["OSC", "3", ";", "Pt", "ST"]} unimplemented="true" />

Set X property on top-level window. `t` should be in the form `prop=value`, or
just `prop` to delete the property.

Ghostty does not implement this OSC.
